site:
  title: Local Preview
content:
  sources:
   - url: .
     branches: HEAD
 
ui:
  bundle:
    url: ./ui-bundle.zip
    snapshot: true
  
output:  
  dir: ./docs
  clean: true
  
#runtime:
#  pull: true
#  cache_dir: ./cache
